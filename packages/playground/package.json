{
  "name": "@nml-lang/playground",
  "version": "0.1.0",
  "description": "Browser-based interactive REPL for the NML language",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "deploy": "vite build && wrangler pages deploy dist --project-name nml-playground",
    "dev": "vite",
    "preview": "vite preview"
  },
  "dependencies": {
    "@nml-lang/compiler-ts": "workspace:*",
    "monaco-editor": "^0.55.1"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "typescript": "^5.5.0"
  }
}
