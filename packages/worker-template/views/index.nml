doctype.html
html.lang("en")
    head
        meta.charset("UTF-8")
        meta.name("viewport").content("width=device-width, initial-scale=1.0")
        title("{{ title }}")
    body
        h1("Hello from NML!")
        p("Edit views/index.nml to get started.")
