{
  "name": "nml",
  "version": "2.2.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test:all": "bun run --filter '*' test",
    "build:all": "bun run --filter '@nml-lang/compiler-ts' build && bun run --filter '@nml-lang/router' build && bun run --filter 'vite-plugin-nml' build && bun run --filter '@nml-lang/cli' build && bun run --filter '@nml-lang/mcp-server' build",
    "typecheck": "bun run --filter '*' tsc --noEmit"
  }
}
